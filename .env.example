# WeeWar Environment Configuration
# Copy this file to configs/.env.dev (development) or configs/.env (production)
# and fill in your credentials.

# =============================================================================
# Basic Configuration
# =============================================================================

# Environment: dev or production
WEEWAR_ENV=dev

# Base URL for the application (used in email verification links, OAuth callbacks)
WEEWAR_BASE_URL=http://localhost:8080

# Server ports
WEEWAR_GRPC_PORT=:9090
WEEWAR_WEB_PORT=:8080

# =============================================================================
# Storage Configuration
# =============================================================================

# Backend storage options: local, pg (PostgreSQL), gae (Google App Engine/Datastore)
WORLDS_SERVICE_BE=local
GAMES_SERVICE_BE=local
FILESTORE_BE=local

# PostgreSQL connection (required if using pg backend)
# WEEWAR_DB_ENDPOINT=postgres://postgres:password@localhost:5432/weewardb

# Google Cloud (required if using gae backend)
# GAE_PROJECT=your-gcp-project-id
# GAE_NAMESPACE=optional-namespace

# R2 storage (required if using r2 filestore)
# R2_ACCOUNT_ID=your-account-id
# R2_ACCESS_KEY_ID=your-access-key
# R2_SECRET_ACCESS_KEY=your-secret-key

# =============================================================================
# User/Auth Storage
# =============================================================================

# Path to store user data (identities, channels, tokens)
# Defaults to ~/dev-app-data/weewar/storage if not set
WEEWAR_USER_STORAGE_PATH=

# =============================================================================
# OAuth2 Authentication
# =============================================================================
# Configure OAuth providers by creating apps in their developer consoles.
# The callback URLs should match your WEEWAR_BASE_URL.

# --- Google OAuth2 ---
# Create app at: https://console.cloud.google.com/apis/credentials
# Add authorized redirect URI: {WEEWAR_BASE_URL}/auth/google/callback
OAUTH2_GOOGLE_CLIENT_ID=
OAUTH2_GOOGLE_CLIENT_SECRET=
OAUTH2_GOOGLE_CALLBACK_URL=http://localhost:8080/auth/google/callback

# --- GitHub OAuth2 ---
# Create app at: https://github.com/settings/developers
# Add authorization callback URL: {WEEWAR_BASE_URL}/auth/github/callback
OAUTH2_GITHUB_CLIENT_ID=
OAUTH2_GITHUB_CLIENT_SECRET=
OAUTH2_GITHUB_CALLBACK_URL=http://localhost:8080/auth/github/callback

# =============================================================================
# Email Configuration (for verification and password reset)
# =============================================================================
# For development, emails are logged to console.
# For production, configure an email service (SMTP, SendGrid, etc.)

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# =============================================================================
# Feature Flags
# =============================================================================

# Hide games/worlds sections in UI
WEEWAR_HIDE_GAMES=false
WEEWAR_HIDE_WORLDS=false
